{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"type": "object",
	"properties": {
		"noResubsOnTrain": {
			"descriptopn": "Prevents resubs from incrementing the sub train.",
			"type": "boolean",
			"default": false
		}
	}
}
